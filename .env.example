# Blockchain RPC URL (Optional - defaults to built-in local node)
# BLOCKCHAIN_RPC_URL=http://127.0.0.1:8545

# Port (optional, defaults to 5000)
PORT=5000

# Node Version (for Render)
NODE_VERSION=18.0.0
